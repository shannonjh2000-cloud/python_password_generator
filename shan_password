# Write a function called shan_password that takes a string as an argument and inserts "shan" in the middle of the string, and then returns the new shan_password.
def shan_password(string):
  middle_index = len(string) // 2
  left = string[:middle_index]
  right = string[middle_index:]
  
  result = left + 'shan' + right
  return result

print(shan_password('Hello World!'))
